import styles from '@/styles/hero.module.css'

export default function Hero() {
  return <div className={styles.hero}>
        <p>TV Show and web series database. Create personalised schedules. Episode guide, crew and character information.</p>
    </div>
}